{
  "name": "subsequent-series",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node -e \"try { require('fs').unlinkSync('dist/server/wrangler.json') } catch(e) {}\"",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.7.0",
    "astro": "^6.4.6",
    "wrangler": "^4.100.0"
  },
  "overrides": {
    "vite": "^7"
  }
}